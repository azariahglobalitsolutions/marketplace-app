services:
  - type: web
    name: habesha-events-usa
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn your_application.wsgi --bind 0.0.0.0:$PORT
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: FLASK_ENV
        value: production
