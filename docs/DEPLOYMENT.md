# Deployment (planned)

The repository is mid-migration:

- **Archived:** Flask app in [`legacy-python/`](../legacy-python/)
- **Scaffold:** [`frontend/`](../frontend/) (Next.js) and [`backend/`](../backend/) (Spring Boot)

Deployment to Render (or similar) will be documented here once the new stack is implemented. The previous Flask deployment notes are preserved in git history and on the `flask-backup` branch.
